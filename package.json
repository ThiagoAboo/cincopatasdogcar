{
  "name": "cinco-patas",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server 8080 --bind 0.0.0.0",
    "build": "echo \"Static site, no build step.\"",
    "build:dev": "echo \"Static site, no build step.\"",
    "start": "python3 -m http.server 8080 --bind 0.0.0.0"
  }
}
